// spiega la dimensione dei tensori
// random tensors
why random tensors are important because the way
many neural networks work..

// create a random tensor of shape size(3, 4)
random_tensor = torch.rand(3, 4)

random_image_size_tensor = torch.rand(size=3, 224, 224)
random_image_size_tensor.shape  # per visualizzare
