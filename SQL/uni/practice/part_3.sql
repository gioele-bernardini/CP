
-- ### Parte 3
-- 1. Perché è sbagliato? Restituire tutte le righe che hanno NULL in D.
-- 2. Cosa restituisce? SELECT C*10 FROM T.
-- 3. Cosa restituisce? SELECT COUNT(*) FROM R.
-- 4. Cosa restituisce? SELECT COUNT(F) FROM R.
-- 5. Cosa restituisce? SELECT SUM(F) FROM R.
-- 6. Cosa restituisce? SELECT D, COUNT(E) FROM U GROUP BY D.
-- 7. Per ogni lingua, il numero di film in quella lingua.
-- 8. Per ogni lingua, la durata totale in minuti dei film in quella lingua.
-- 9. Per ogni attore i cui film durano tutti <= 170 minuti, il numero di film in cui ha recitato.

-- Queste sono tutte le domande richieste nei tre documenti PDF forniti.

